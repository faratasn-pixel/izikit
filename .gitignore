node_modules/
dist/
.next/
.turbo/
coverage/
*.log
.DS_Store
.env
.env.local
.env.*.local
*.tsbuildinfo
.prisma/
next-env.d.ts
.vercel

# Claude Code — keep .claude/skills/ + settings.json shared, ignore per-machine state
.claude/settings.local.json
.claude/worktrees/
.claude/skills/ui-ux-pro-max/scripts/__pycache__/

# Local cruft — not part of the kit
amp
node-compile-cache/
# Bundle de déploiement assemblé par la CI (jamais commité)
/deploy/
# Fichier d'env de production (déposé manuellement sur le serveur)
/.env

# Local PostgreSQL cluster for dev (avoids burning Neon's free compute-hour
# quota) — portable binaries + data directory, per-machine, never committed.
.pg-local/

